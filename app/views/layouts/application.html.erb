<!DOCTYPE html>
<html>
<head>
  <title><%= content_for(:title) || "Movie Log" %></title>
  <meta name="viewport" content="width=device-width,initial-scale=1">
  <meta name="apple-mobile-web-app-capable" content="yes">
  <meta name="application-name" content="Movie Log">
  <meta name="mobile-web-app-capable" content="yes">
  <%= csrf_meta_tags %>
  <%= csp_meta_tag %>

  <%= yield :head %>

  <%# Enable PWA manifest for installable apps (make sure to enable in config/routes.rb too!) %>
  <%#= tag.link rel: "manifest", href: pwa_manifest_path(format: :json) %>

  <link rel="icon" href="/icon.png" type="image/png">
  <link rel="icon" href="/icon.svg" type="image/svg+xml">
  <link rel="apple-touch-icon" href="/icon.png">

  <!-- bootstrap -->
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>

  <%# Includes all stylesheet files in app/assets/stylesheets %>
  <%= stylesheet_link_tag :app, "data-turbo-track": "reload" %>
  <%= javascript_importmap_tags %>
</head>

<body>
<%= yield %>
<!-- toast notification -->
<% if notice.present? %>
  <div class="position-fixed bottom-0 end-0 p-3" style="z-index: 1050">
    <div id="toast-notice" class="toast align-items-center text-bg-success border-0 show" role="alert" aria-live="assertive" aria-atomic="true">
      <div class="toast-body">
        <%= notice %>
      </div>
    </div>
  </div>
<% end %>

<!-- dismiss toast notification -->
<script>
  document.addEventListener("turbo:load", () => {
      const toastNotification = document.getElementById("toast-notice");
      if (toastNotification) {
          const toast = new bootstrap.Toast(toastNotification, {delay: 2500});
          toast.show();
      }
  })
</script>
</body>
</html>
